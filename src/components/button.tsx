'use client'

import React from 'react';

export const Button = () => {
    return (
        <button className={'bg-amber-800 rounded-sm w-30 cursor-pointer'} type={'button'}>
            PRESS ME
        </button>
    );
};
